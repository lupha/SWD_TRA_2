# learnings

für die Installation von nestjs müssen die Schreibrechte der Verzeichnisse: /usr/local/lib , include und bin vorhanden sein

für docker muss Windows professional installiert sein

wichtig bei Dockerfiles From node:12-alpine und nicht node: 12-alpine

es darf nur ein Docker Container mit dem selben Namen besetehen